#pseudo code of prototype of slahsing subsamples

def slash_samples(key, value):
   # if the subsample doen't have the attestation majority of de inequality (major) raise a slashing
    if inequality < attestation #raise Exception
    # cheack research in genetic algo in distributed systems
    # in weird summary ACS honeybadger with subsamples 
    
